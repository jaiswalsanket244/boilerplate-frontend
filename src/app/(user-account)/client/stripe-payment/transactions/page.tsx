import TransactionsStripePayment from "@/module/stripe-payment/templates/transactions-stripe-payment";

const StripePaymentTransactions = () => {
	return <TransactionsStripePayment />;
};

export default StripePaymentTransactions;
