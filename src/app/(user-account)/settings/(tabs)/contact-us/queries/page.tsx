import PreviousQueries from "@/module/profile/templates/previous-queries";
import { Suspense } from "react";

const PreviousQueriesPage = () => {
	return (
		<Suspense>
			<PreviousQueries />
		</Suspense>
	);
};

export default PreviousQueriesPage;
